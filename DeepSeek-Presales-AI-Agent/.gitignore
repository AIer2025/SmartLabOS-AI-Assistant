# 构建产物
bin/
obj/
publish/
*.user

# IDE
.vs/
.idea/
*.suo

# 机密（绝不入仓）：本地开发用 user-secrets(存于用户配置目录)或环境变量注入
# env 配置文件含 MySQL 口令 → 只提交 .sample.json 模板，现场按模板另存为正式文件
env/Presales-AI-Agent-config.json
appsettings.Development.json
appsettings.Production.json
appsettings.*.local.json
secrets.json
*.env

# 发布产物（dotnet publish 输出，可随时重新生成；曾因含真实密钥被 GitHub 推送保护拦截）
IIS-Publish/

# 含真实密钥的运行配置，无论放在哪个目录都不入仓（只提交 .sample.json 模板）
# 注意：原来只写了 env/ 路径，导致 IIS-Publish/ 下的同名副本漏网
Presales-AI-Agent-config.json
