{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//配置说明": [
    "MySQL 连接参数与 ClaudeAI 运行目录已全部外置到 env/Presales-AI-Agent-config.json（项目根的 env 目录），",
    "由 AgentEnvFile 在启动时读取并注入最高优先级配置层，因此本文件不再保留 ConnectionStrings 与 Presales 路径节。",
    "改配置只改那一个 JSON，不需要重新编译；启动日志会打印它的实际读取路径与生效的各个目录。",
    "可用环境变量 PRESALES_AGENT_CONFIG 指定该 JSON 的完整路径（用于多环境并存）。",
    "Anthropic API Key 不进任何配置文件：用环境变量 ANTHROPIC_API_KEY 或 dotnet user-secrets Agent:ApiKey 注入。"
  ],

  "//Agent说明": "大模型运行参数（与部署环境无关，故留在本文件）。ApiKey 请勿写在此处。",
  "Agent": {
    "Model": "claude-opus-4-8",
    "Effort": "xhigh",
    "MaxTokens": 64000,
    "TimeoutMinutes": 60,
    "MaxToolIterations": 40,
    "MaxConcurrency": 2,
    "MaxAttempts": 5,
    "CacheTtl": "1h",
    "EnableDocx": true,
    "PandocPath": ""
  }
}
